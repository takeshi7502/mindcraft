# Default combat behavior

The default and survival profiles start with `hunting` disabled, so the bot does not opportunistically attack nearby animals while idle.

Automatic `self_defense` remains enabled and unchanged. Explicit player commands such as `!attack("cow")` and `!attackPlayer("name")` also remain available. Custom profiles can still override modes deliberately.